---
phase: 11-visual-enrichment-and-trip-sharing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/itinerary/ActivityCard.tsx
  - src/components/itinerary/DaySection.tsx
  - src/components/landing/DestinationCards.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Activity cards on the itinerary page show a photo at the top when extra_data.photo_url is set"
    - "Activity cards show star rating and review count when extra_data.places_rating is set"
    - "Activity cards show price level as $ / $$ / $$$ / $$$$ when extra_data.places_price_level is set"
    - "No activity card uses backdropFilter: blur() — all activity cards use solid rgba backgrounds (VideoHero and ItineraryHero are NOT changed)"
    - "Destination cards on the landing page use next/image instead of <img> tags"
    - "Destination cards use brand palette colors (navy, coral, umber) not gray-* classes"
  artifacts:
    - path: "src/components/itinerary/ActivityCard.tsx"
      provides: "ActivityCard with photo header and rating row, no backdropFilter"
    - path: "src/components/itinerary/DaySection.tsx"
      provides: "ActivityCardItem with photo + rating in expanded view, no backdropFilter on any card"
    - path: "src/components/landing/DestinationCards.tsx"
      provides: "Destination cards using next/image, brand palette colors"
  key_links:
    - from: "src/components/itinerary/DaySection.tsx ActivityCardItem"
      to: "activity.extra_data.photo_url"
      via: "typed cast of extra_data as Record<string, unknown>"
      pattern: "extra_data.*photo_url"
    - from: "src/components/itinerary/DaySection.tsx ActivityCardItem"
      to: "activity.extra_data.places_rating"
      via: "typed cast of extra_data as Record<string, unknown>"
      pattern: "extra_data.*places_rating"
---

<objective>
Remove `backdropFilter: blur()` from activity card components ONLY (GPU performance fix) and replace with a modern photo-first card design that shows Unsplash photos and Google Places ratings from `extra_data`. Upgrade landing page destination cards to `next/image` with brand palette compliance.

**SCOPE CONSTRAINT:** Glassmorphism removal is limited to activity cards (`DaySection.tsx`, `ActivityCard.tsx`). Do NOT touch `VideoHero.tsx`, `ItineraryHero.tsx`, or any other hero/landing page components — the blur effects there are intentional and must be preserved.

Purpose: The activity card glassmorphism creates ~20 GPU compositing layers during scroll, causing frame drops on mobile. Photos and ratings are the emotional and utility triggers that close the "formatted list, not a trip" gap. Both changes are independent of the data pipeline (they render from existing extra_data or gracefully omit).
Output: Visually redesigned activity cards (modern, magazine-quality), performance-safe destination cards.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-visual-enrichment-and-trip-sharing/11-CONTEXT.md
@.planning/phases/11-visual-enrichment-and-trip-sharing/11-RESEARCH.md
@src/components/itinerary/DaySection.tsx
@src/components/itinerary/ActivityCard.tsx
@src/components/landing/DestinationCards.tsx
</context>

<interfaces>
<!-- Brand palette (enforced — no blue-* classes) -->
<!-- navy: #285185, coral: #D67940, umber: #6F4849, sky: #CCD9E2, sand: #F5EDE3 -->

<!-- Activity type interface -->
```typescript
// src/lib/types.ts (relevant fields)
interface Activity {
  id: string
  name: string
  time: string | null
  description: string | null
  location: string | null
  activity_type: string | null
  extra_data: unknown  // cast to Record<string, unknown> to read photo_url, places_rating etc.
}
// extra_data keys added by Phase 11 pipeline (may or may not exist on any given activity):
// photo_url: string          — Unsplash images.unsplash.com URL
// places_rating: number      — e.g. 4.6
// places_review_count: number — e.g. 2847
// places_price_level: number  — 0-4, display as Free/$/$$/$$$/$$$$
```

<!-- next/image usage pattern already established in ItineraryHero.tsx -->
```typescript
import Image from 'next/image'
// <Image src={url} alt={alt} fill className="object-cover" />
// For fixed-height containers: wrap in <div className="relative h-32 w-full">
// next.config.ts already has images.unsplash.com in remotePatterns — no config change needed
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove glassmorphism, add photo+rating display to activity cards in DaySection and ActivityCard</name>
  <files>src/components/itinerary/DaySection.tsx, src/components/itinerary/ActivityCard.tsx</files>
  <action>
    In DaySection.tsx — ActivityCardItem component:
    1. REMOVE `backdropFilter: 'blur(12px)'` from the card's inline style object (line ~92). Replace with: no backdropFilter property at all. The background values are already solid rgba — keep them as-is (rgba(255,255,255,0.95) active, rgba(255,255,255,0.88) expanded, rgba(255,255,255,0.65) default).
    2. ADD photo display at the TOP of the card (before the main row), visible when card has a photo_url AND is not a hotel:
       - Read: `const photoUrl = !isHotel ? (activity.extra_data as Record<string, unknown> | null)?.photo_url as string | undefined : undefined`
       - Render (above the existing main row div): if photoUrl, render `<div className="relative w-full overflow-hidden rounded-t-xl" style={{ height: '128px' }}><Image src={photoUrl} alt={activity.name} fill className="object-cover" sizes="(max-width: 768px) 100vw, 50vw" /></div>`
       - Import `Image` from 'next/image' at the top of the file
    3. ADD rating row in the expanded section, after location and before description:
       - Read: `const placesRating = (activity.extra_data as Record<string, unknown> | null)?.places_rating as number | undefined`
       - Read: `const placesReviewCount = (activity.extra_data as Record<string, unknown> | null)?.places_review_count as number | undefined`
       - Read: `const placesPriceLevel = (activity.extra_data as Record<string, unknown> | null)?.places_price_level as number | undefined`
       - If any of the three are defined, render a rating row: `<div className="flex items-center gap-2 text-[11px]">` containing:
         - If placesRating: `<span style={{ color: '#D67940' }}>★ {placesRating.toFixed(1)}</span>` + if placesReviewCount: `<span style={{ color: 'rgba(111,72,73,0.5)' }}>({placesReviewCount.toLocaleString()})</span>`
         - If placesPriceLevel defined (0-4): `<span style={{ color: 'rgba(40,81,133,0.5)' }}>{PRICE_SYMBOLS[placesPriceLevel]}</span>` where PRICE_SYMBOLS = { 0: 'Free', 1: '$', 2: '$$', 3: '$$$', 4: '$$$$' } (define as a const above the component)
    4. When the card has a photo (photoUrl is set), change the card's border-radius on the card div to still use `rounded-xl` — the photo already has `rounded-t-xl` so the top corners look natural. The inner card div already has `overflow-hidden` which clips the photo corners correctly.

    In ActivityCard.tsx (used in ContextPanel / chat itinerary preview):
    1. REMOVE `backdropFilter: 'blur(12px)'` from the motion.div style (line ~23). Keep the background rgba values unchanged.
    2. ADD photo display at the top of the card, same pattern as DaySection: read `extra_data?.photo_url`, if set render `<div className="relative w-full overflow-hidden rounded-t-2xl" style={{ height: '112px' }}><Image ... /></div>` before the `<div className="flex items-start gap-3 pl-1">` block.
    3. REMOVE the existing `layout` prop from motion.div (it triggers layout animation which is expensive with many cards). Use `whileHover` and `whileTap` only.
    4. Import Image from 'next/image'.

    Do NOT change any other visual properties (colors, spacing, font sizes, shadows, borders). The goal is surgical: remove blur, add photo above, add rating in expanded section.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/activity-row.test.tsx src/__tests__/itinerary-detail.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - No `backdropFilter` remains in ActivityCard.tsx or DaySection.tsx
    - ActivityCardItem renders `<Image>` when extra_data.photo_url is set
    - Rating row renders when extra_data.places_rating is set
    - Price level displays as $ symbols
    - All existing tests still pass
    - npx tsc --noEmit clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade DestinationCards to next/image and brand palette</name>
  <files>src/components/landing/DestinationCards.tsx</files>
  <action>
    1. Replace `import Link from 'next/link'` — keep. Add `import Image from 'next/image'`.
    2. In DESTINATIONS array, add `width: 800, height: 480` fields to each entry (needed for next/image with local public paths). These are the display dimensions hint — actual file may be any size.
    3. Replace the `<img>` tag inside the card with:
       ```tsx
       <Image
         src={dest.image}
         alt={dest.title}
         width={800}
         height={480}
         className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
       />
       ```
    4. Fix brand palette violations — replace gray-* and text-gray-* with brand colors:
       - `text-gray-400` → inline style `color: 'rgba(111,72,73,0.5)'` (umber subdued)
       - `text-gray-900` (card title h3) → inline style `color: '#285185'` (navy)
       - `text-gray-500` → inline style `color: 'rgba(111,72,73,0.6)'` (umber)
       - `text-gray-900` (section heading h2 "Where to go next") → keep `font-serif` class, use inline style `color: '#285185'`
       - `text-gray-900` (value prop h3 "All-in-One AI Trip Planner") → inline style `color: '#285185'`
       - `text-gray-500` (value prop paragraph) → inline style `color: 'rgba(111,72,73,0.6)'`
       - `bg-navy-light` on the CTA button does not exist in Tailwind config — change to inline style `backgroundColor: '#285185'` and add hover via Tailwind class `hover:opacity-90`
    5. The "Start planning →" link is currently `text-gray-900 hover:text-coral` — change to inline style `color: '#D67940'` + remove the gray hover (always coral, matches brand CTAs).
    6. Keep the card layout, motion variants, and shadow unchanged. Only swap img→Image and fix colors.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/video-hero.test.tsx --reporter=verbose && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - No `<img>` tags remain in DestinationCards.tsx
    - No `text-gray-*` or `bg-gray-*` classes remain in DestinationCards.tsx
    - next/image used for all destination photos
    - Brand palette applied: navy headings, coral links/CTAs, umber subdued text
    - npx tsc --noEmit clean
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose — all existing tests pass.
npx tsc --noEmit — no new TypeScript errors.
Visual spot-check: run dev server, open /itinerary/[id] — no backdrop blur visible on activity cards. Open landing page — destination cards use next/image, no gray text.
</verification>

<success_criteria>
- Zero backdropFilter blur on activity cards (DaySection.tsx and ActivityCard.tsx)
- photo_url from extra_data renders as next/image at top of card when present
- places_rating, places_review_count, places_price_level render in expanded card section
- DestinationCards.tsx uses next/image, brand palette throughout
- All existing Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-enrichment-and-trip-sharing/11-02-SUMMARY.md`
</output>
