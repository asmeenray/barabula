---
phase: 10-rich-itinerary-ai-response-detailed-daily-plans-with-timings-flights-hotels-restaurants-and-editable-tiles
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/ai/prompts/trip-planner.ts
  - src/app/api/chat/message/route.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "AI system prompt instructs clock-time-only format for all activity times"
    - "AI system prompt instructs ascending time sort within each day"
    - "AI system prompt instructs hotel star rating inference from travel_style/budget"
    - "AI system prompt generates flights array (outbound + return) with is_suggested logic"
    - "AI system prompt generates daily_food array (dinner + local tip per day)"
    - "Chat message route persists flights and daily_food into itinerary extra_data on itinerary_complete"
    - "Chat message route persists duration and tips on each inserted activity"
    - "max_tokens bumped to 8192 to accommodate richer schema"
    - "extra_data merge is safe — flights and daily_food stored together without overwriting each other"
  artifacts:
    - path: "src/lib/ai/prompts/trip-planner.ts"
      provides: "Updated TRIP_PLANNER_RULES with clock-time mandate, hotel star inference, flights section, daily_food section"
      contains: "clock time"
    - path: "src/app/api/chat/message/route.ts"
      provides: "Persists duration/tips per activity, flights+daily_food in itinerary extra_data, max_tokens 8192"
      contains: "extra_data: { flights:"
  key_links:
    - from: "src/app/api/chat/message/route.ts"
      to: "supabase itineraries.extra_data"
      via: "supabase.from('itineraries').update({ extra_data: { flights, daily_food } })"
      pattern: "extra_data.*flights"
    - from: "src/app/api/chat/message/route.ts"
      to: "supabase activities"
      via: "activities.insert() includes duration and tips fields per activity"
      pattern: "duration.*act\\.duration"
---

<objective>
Update the AI system prompt to generate richer, clock-time-sorted itineraries with flights and daily food recommendations. Update the chat message route to persist the new fields into the database: duration/tips per activity, and flights/daily_food into itinerary extra_data.

Purpose: Without this plan, the UI has nowhere to read flight or food data from. The AI must emit it and the route must save it.
Output: Richer AI responses persisted correctly with no data loss on extra_data merge.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/ai/prompts/trip-planner.ts
@src/app/api/chat/message/route.ts
@src/lib/ai/schemas.ts
@src/lib/types.ts

@.planning/phases/10-rich-itinerary-ai-response-detailed-daily-plans-with-timings-flights-hotels-restaurants-and-editable-tiles/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Current trip-planner.ts structure (read in plan 01 context) -->
<!-- TRIP_PLANNER_RULES string ends with Formatting Rules section -->
<!-- Insert new rule sections before the closing backtick of TRIP_PLANNER_RULES -->

<!-- Current message route persistence block (lines 118-139 of route.ts) -->
<!-- activities insert currently maps: name, time, description, location, activity_type, extra_data -->
<!-- ADD: duration: act.duration ?? null, tips: act.tips ?? null -->

<!-- itinerary insert currently maps: user_id, ...itineraryFields (which destructures out 'days') -->
<!-- flights and daily_food are top-level on parsed.itinerary — destructure them out alongside days -->
<!-- Then PATCH itineraries with extra_data: { flights: [...], daily_food: [...] } AFTER the insert -->
<!-- Pattern from RESEARCH.md Pitfall 3: read existing extra_data, spread, then write -->
<!-- Since this is a fresh insert, extra_data will be '{}' — safe to set directly on first write -->

<!-- max_tokens: currently 4096 at line 41 — change to 8192 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update AI system prompt with Phase 10 rules</name>
  <files>src/lib/ai/prompts/trip-planner.ts</files>
  <action>
    Update `TRIP_PLANNER_RULES` in `src/lib/ai/prompts/trip-planner.ts`. Add new rule sections. Keep all existing rules intact — only append to the string.

    **Replace the Hotel Accommodation Rules section** with an expanded version:

    ```
    ## Hotel Accommodation Rules
    - For each day in the itinerary, include exactly ONE activity with activity_type: "hotel" representing the day's accommodation.
    - Set hotel_name (e.g. "Park Hyatt Tokyo"), star_rating (1–5 integer), and location (hotel address or neighbourhood).
    - Infer star_rating from travel_style and budget:
      - luxury / high-end → 5-star
      - mid / moderate → 4-star
      - budget / backpacker → 3-star
      - unknown or not specified → default 4-star
    - For check_in and check_out: use specific clock times where possible (e.g. "3:00 PM", "11:00 AM"). If check-in is on a different day, state the date + time (e.g. "15 March, 3:00 PM").
    - Set activity_type: "activity" for all non-hotel activities.
    - Set hotel_name, star_rating, check_in, and check_out to null for all activity_type: "activity" entries.
    ```

    **Replace the time-related rule** in The Four Intake Fields / Phase Rules (currently says "morning/afternoon/evening") with a Time Format section:

    Add as a new section after Hotel Accommodation Rules:

    ```
    ## Time Format Rules (STRICT)
    - ALL activity times must use specific clock times: "9:00 AM", "2:30 PM", "7:00 PM" — NEVER use "morning", "afternoon", "evening", "night", or "All day" as a time value.
    - Activities within each day MUST be in ascending time order (earliest first).
    - Hotel check-in and check-out times must also use clock times (e.g. "3:00 PM", "11:00 AM").
    - duration field: plain English string like "2–3 hours" or "45 minutes" — NOT calculated, just estimated. Leave null if unknown.
    - tips field: one practical tip only when genuinely useful (e.g. "Book tickets online 2 weeks ahead"). Leave null for most activities — do NOT force a tip on every activity.
    ```

    **Add a new Flights Rules section** after Time Format Rules:

    ```
    ## Flights Rules
    - Always generate a flights array with exactly two entries: one outbound and one return flight.
    - Outbound flight: direction "outbound", logically tied to Day 1.
    - Return flight: direction "return", logically tied to the last day.
    - Use realistic airlines and airports based on the trip origin and destination.
    - departure_time and arrival_time: use specific clock times with timezone context where known (e.g. "7:30 AM", "11:45 AM").
    - is_suggested: always set to true (AI-generated suggestions). The application overrides this to false if the user explicitly provided flight details.
    - If origin city is null: use the most common departure hub for the destination country.
    - Set flight_number to a plausible code (e.g. "BA287") — clearly a suggestion, not a booking.
    ```

    **Add a new Daily Food Rules section** after Flights Rules:

    ```
    ## Daily Food Rules
    - Generate a daily_food array with one entry per trip day (same count as days[]).
    - Each entry: day_number (integer matching the day), dinner_restaurant (specific restaurant name), dinner_area (neighbourhood or area), dinner_cuisine (e.g. "Japanese", "Italian", "Street food"), local_tip (one food tip for that area — can be about street food, markets, or a local secret).
    - dinner_restaurant must be a real or highly plausible restaurant for the destination, not generic.
    - local_tip must be specific and practical (e.g. "Try the tsukemen ramen at the stalls near Shinjuku Station"). Leave null if nothing specific comes to mind.
    ```

    Also update the itinerary phase rule that says "morning/afternoon/evening activities" — find the line:
    `- The itinerary should have morning/afternoon/evening activities with practical details.`
    and replace it with:
    `- The itinerary should have activities at specific clock times (9:00 AM, 2:30 PM, etc.) in ascending time order per day, with practical details.`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>trip-planner.ts contains Time Format Rules, Flights Rules, Daily Food Rules sections. Hotel star inference from travel_style/budget documented. "morning/afternoon/evening" wording replaced with clock-time mandate. Full test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update chat message route — persist duration/tips/flights/daily_food</name>
  <files>src/app/api/chat/message/route.ts</files>
  <action>
    Update `src/app/api/chat/message/route.ts` with three changes:

    **Change 1: Bump max_tokens to 8192** (currently 4096 at the openai.chat.completions.parse call):
    ```typescript
    max_tokens: 8192,
    ```

    **Change 2: Include duration and tips in the activities insert** (currently in the flatMap around lines 119-137).
    The activities map currently includes: name, time, description, location, activity_type, extra_data.
    Add after location:
    ```typescript
    duration: act.duration ?? null,
    tips: act.tips ?? null,
    ```

    **Change 3: Persist flights and daily_food into itinerary extra_data** after the activities insert succeeds.
    Currently the block destructures `const { days, ...itineraryFields } = parsed.itinerary`.
    Change to also destructure flights and daily_food:
    ```typescript
    const { days, flights, daily_food, ...itineraryFields } = parsed.itinerary
    ```

    Then after the cover image fetch block (and before the activities insert), add:
    ```typescript
    // Persist flights and daily_food into itinerary extra_data
    // Note: this is a fresh insert so extra_data is '{}' — safe to set directly.
    // For future edits, the PATCH route reads existing extra_data first and merges (Pitfall 3 pattern).
    if (flights?.length || daily_food?.length) {
      await supabase
        .from('itineraries')
        .update({
          extra_data: {
            flights: flights ?? [],
            daily_food: daily_food ?? [],
          }
        })
        .eq('id', newItinerary.id)
    }
    ```

    IMPORTANT: Do NOT change any other part of the route. Preserve the session upsert, phase guard, cover image fetch, and all existing error handling. The existing `extra_data` field on activity insert (hotel extra_data) stays unchanged.

    Do NOT use blue-* Tailwind classes. This is API route TypeScript — no UI code.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/chat.test.ts --reporter=verbose</automated>
  </verify>
  <done>max_tokens is 8192. Activities insert includes duration and tips. flights/daily_food destructured from parsed.itinerary and persisted to extra_data. Existing chat API tests still pass. No TypeScript errors (npx tsc --noEmit).</done>
</task>

</tasks>

<verification>
- `grep "max_tokens" src/app/api/chat/message/route.ts` returns `8192`
- `grep "duration.*act\\.duration" src/app/api/chat/message/route.ts` shows the field mapped
- `grep "extra_data.*flights" src/app/api/chat/message/route.ts` shows the persist block
- `grep "Clock\|clock time\|is_suggested\|daily_food" src/lib/ai/prompts/trip-planner.ts` — all four present
- `npx vitest run` — no new test failures above the 4 pre-existing baseline
</verification>

<success_criteria>
- max_tokens bumped to 8192 in the message route
- Activity inserts include duration and tips fields
- flights and daily_food arrays from AI response persisted to itinerary extra_data
- System prompt enforces clock-time-only format, ascending time sort, hotel star inference, flights generation, and daily_food generation
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-rich-itinerary-ai-response-detailed-daily-plans-with-timings-flights-hotels-restaurants-and-editable-tiles/10-02-SUMMARY.md`
</output>
