---
phase: 12-mobile-responsive-web-single-column-chat-on-mobile-hide-right-panel-navigate-to-itinerary-after-chat
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/split-layout.test.tsx
  - src/__tests__/chat-page.test.tsx
  - src/__tests__/itinerary-detail.test.tsx
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-03

must_haves:
  truths:
    - "split-layout.test.tsx tests reflect actual SplitLayout implementation (flex h-full, not grid-cols-2 or h-screen)"
    - "split-layout.test.tsx has a test case asserting right section has hidden md:flex class after the responsive change"
    - "chat-page.test.tsx has a test case asserting the mobile overlay renders when showMobileOverlay state is true"
    - "itinerary-detail.test.tsx has a test case asserting the Chat again FAB renders with md:hidden class"
    - "npx vitest run passes (no red tests) after Wave 0 before any implementation begins"
  artifacts:
    - path: "src/__tests__/split-layout.test.tsx"
      provides: "Updated + new SplitLayout test cases"
      contains: "hidden md:flex"
    - path: "src/__tests__/chat-page.test.tsx"
      provides: "Mobile overlay test scaffold"
      contains: "mobile overlay"
    - path: "src/__tests__/itinerary-detail.test.tsx"
      provides: "Chat again FAB test case"
      contains: "Chat again"
  key_links:
    - from: "src/__tests__/split-layout.test.tsx"
      to: "src/components/chat/SplitLayout.tsx"
      via: "getByTestId('split-layout')"
      pattern: "split-layout"
    - from: "src/__tests__/itinerary-detail.test.tsx"
      to: "src/app/(authenticated)/itinerary/[id]/page.tsx"
      via: "getByRole('button', { name: /chat again/i })"
      pattern: "Chat again"
---

<objective>
Fix two stale tests in split-layout.test.tsx that check for classes that don't exist in the current implementation (grid-cols-2, h-screen). Add test scaffolds for the three new behaviors this phase introduces: SplitLayout responsive hiding, the mobile navigation overlay in chat, and the Chat again FAB on the itinerary page.

Purpose: Wave 0 must complete before Wave 1 implementation so tests are green before changes are made and red-green-refactor is valid. A pre-existing failing test would mask implementation regressions.
Output: Three updated test files; all tests passing; explicit scaffold tests that will fail until implementation is complete (MISSING sentinel tests are acceptable at Wave 0 — they drive Wave 1).
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mobile-responsive-web-single-column-chat-on-mobile-hide-right-panel-navigate-to-itinerary-after-chat/12-CONTEXT.md
@.planning/phases/12-mobile-responsive-web-single-column-chat-on-mobile-hide-right-panel-navigate-to-itinerary-after-chat/12-RESEARCH.md

<interfaces>
<!-- Current SplitLayout.tsx — what tests should actually assert against -->
<!-- The container uses: className="flex h-full overflow-hidden" data-testid="split-layout" -->
<!-- Left panel: className="flex flex-col h-full overflow-hidden" with inline style={{ width }} -->
<!-- Drag handle: className="group relative shrink-0 w-1.5 h-full cursor-col-resize ..." -->
<!-- Right panel: className="relative h-full overflow-hidden bg-gray-950" with inline style={{ width }} -->

<!-- After Plan 02, SplitLayout right section wrapper will have: className="hidden md:flex h-full" -->
<!-- Left panel will have: className="flex-1 flex flex-col h-full overflow-hidden md:flex-none" -->

<!-- Current chat/page.tsx — ChatPageInner has: -->
<!-- const [sending, setSending] = useState(false) -->
<!-- const [fullItinerary, setFullItinerary] = useState<FullItineraryData | null>(null) -->
<!-- callApi() is where data.itineraryId handling lives (line 169+) -->
<!-- AnimatePresence already imported from 'motion/react' -->

<!-- Current itinerary/[id]/page.tsx — after Plan 03 a button will be added: -->
<!-- aria-label="Chat again to refine your trip" or text "Chat again" -->
<!-- className includes md:hidden -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix stale SplitLayout tests and add responsive scaffold</name>
  <files>src/__tests__/split-layout.test.tsx</files>
  <behavior>
    - Test "renders both left and right children": keep as-is (already correct)
    - Test "has grid-cols-2 for the 50/50 split": DELETE this test — SplitLayout uses flex, not grid. Replace with "container uses flex layout" asserting className contains "flex"
    - Test "has h-screen for full viewport height": DELETE this test — container uses h-full, not h-screen. Replace with "container uses h-full" asserting className contains "h-full"
    - NEW test "right section is hidden on mobile, visible on desktop": assert right section wrapper has className containing "hidden md:flex" — this test will FAIL until Plan 02 implements the wrapper
    - NEW test "left panel fills full width when right is hidden": assert left panel div has className containing "flex-1" — this test will FAIL until Plan 02 implements flex-1 on left panel
  </behavior>
  <action>
    Rewrite src/__tests__/split-layout.test.tsx to:
    1. Keep the "renders both left and right children" test unchanged.
    2. Remove the stale "grid-cols-2" test. Add "container uses flex layout" that checks getByTestId('split-layout').className contains 'flex'.
    3. Remove the stale "h-screen" test. Add "container uses h-full" that checks getByTestId('split-layout').className contains 'h-full'.
    4. Add test "hides right section on mobile (hidden md:flex)" — render SplitLayout with left and right children. Find the right section wrapper by querying the element that contains the right panel child. Assert its className contains 'hidden' and 'md:flex'. Use container.querySelector to find a div that wraps both drag handle and right panel — after Plan 02 this will be a wrapper div. For now this test will fail (EXPECTED before Plan 02 runs).
    5. Add test "left panel fills width when right is hidden (flex-1)" — assert the left panel div className contains 'flex-1'.

    IMPORTANT: Tests 4 and 5 are intentionally failing (they test the Plan 02 implementation). This is the correct Wave 0 scaffold pattern.

    Use the same import pattern as the existing file: import from 'vitest', '@testing-library/react', '@/components/chat/SplitLayout'.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/split-layout.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    Tests 1-3 pass. Tests 4-5 fail with "does not contain 'hidden md:flex'" / "does not contain 'flex-1'" — expected before Plan 02. Full test output visible and no unexpected errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mobile overlay and FAB scaffolds to existing test files</name>
  <files>src/__tests__/chat-page.test.tsx, src/__tests__/itinerary-detail.test.tsx</files>
  <action>
    Add new test cases to the EXISTING test files. Do not replace any existing tests.

    In src/__tests__/chat-page.test.tsx — add after the last existing test:

    ```
    it('shows mobile overlay when showMobileOverlay state is true', async () => {
      // Note: this test verifies the overlay element exists in the DOM when rendered.
      // The overlay is controlled by showMobileOverlay state inside ChatPageInner.
      // Since we cannot set internal state directly, this test verifies the overlay
      // does NOT appear on initial render (it is hidden by default).
      // The full overlay behavior is validated manually (mobile viewport + itinerary generation).
      mockDefaultFetches()
      render(<ChatPage />)
      // Overlay should not be present on initial load
      expect(screen.queryByText('Building your itinerary...')).not.toBeInTheDocument()
    })
    ```

    NOTE: The mockDefaultFetches helper is defined earlier in the file — reuse it. This test will PASS immediately (overlay absent on load). After Plan 02 implementation, also add a test that triggers the overlay by mocking itineraryId response on a narrow window — but this simpler assertion is sufficient for Wave 0 scaffolding.

    In src/__tests__/itinerary-detail.test.tsx — add to the ItineraryDetailPage describe block after the last existing test:

    ```
    it('renders "Chat again" FAB with md:hidden class (mobile only)', () => {
      ;(useSWR as ReturnType<typeof vi.fn>).mockReturnValue({
        data: mockData,
        error: null,
        isLoading: false,
        mutate: vi.fn(),
      })
      render(<ItineraryDetailPage />)
      const fab = screen.getByRole('button', { name: /chat again/i })
      expect(fab).toBeInTheDocument()
      expect(fab.className).toContain('md:hidden')
    })
    ```

    This test will FAIL until Plan 03 adds the FAB to the itinerary page. Expected before Plan 03.

    Do NOT modify any other tests. Preserve all existing imports and test structure.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/chat-page.test.tsx src/__tests__/itinerary-detail.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>
    chat-page.test.tsx: all existing tests pass, new overlay test passes. itinerary-detail.test.tsx: all existing tests pass, new "Chat again" FAB test fails with "Unable to find an accessible element with the role button and name /chat again/i" — expected before Plan 03.
  </done>
</task>

</tasks>

<verification>
Run the full test suite after Wave 0 to confirm baseline:
- npx vitest run 2>&1 | tail -30
- Expected: all pre-existing tests pass; 3 new scaffold tests fail as documented (split-layout responsive + FAB)
- No unexpected failures
</verification>

<success_criteria>
- split-layout.test.tsx no longer references grid-cols-2 or h-screen
- Two new scaffold tests exist in split-layout.test.tsx (will be made green by Plan 02)
- chat-page.test.tsx has overlay absence test (passes immediately)
- itinerary-detail.test.tsx has Chat again FAB test (will be made green by Plan 03)
- Full test suite: same passing count as before plus 1 new pass; 3 new expected fails documented
</success_criteria>

<output>
After completion, create `.planning/phases/12-mobile-responsive-web-single-column-chat-on-mobile-hide-right-panel-navigate-to-itinerary-after-chat/12-01-SUMMARY.md`
</output>
