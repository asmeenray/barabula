---
phase: 09-itinerary-page-with-map-integration-and-hotel-details
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(authenticated)/itinerary/[id]/page.tsx
  - src/app/(authenticated)/itinerary/new/page.tsx
  - src/components/itinerary/ActivityForm.tsx
  - src/components/itinerary/ActivityRow.tsx
autonomous: true
requirements:
  - BLUE-01
must_haves:
  truths:
    - "No blue-* Tailwind classes exist in any itinerary file"
    - "All form focus rings use focus:ring-coral"
    - "All primary buttons use bg-navy or bg-coral (not bg-blue-*)"
    - "All text highlights use text-coral (not text-blue-*)"
  artifacts:
    - path: "src/app/(authenticated)/itinerary/[id]/page.tsx"
      provides: "Blue-free itinerary detail page"
    - path: "src/app/(authenticated)/itinerary/new/page.tsx"
      provides: "Blue-free new itinerary form"
    - path: "src/components/itinerary/ActivityForm.tsx"
      provides: "Blue-free activity form modal"
    - path: "src/components/itinerary/ActivityRow.tsx"
      provides: "Blue-free activity row"
  key_links:
    - from: "brand palette"
      to: "all itinerary files"
      via: "Tailwind custom colors (coral, navy, umber, sky, sand)"
      pattern: "focus:ring-coral|text-coral|bg-navy"
---

<objective>
Remove all blue-* Tailwind class violations from the four itinerary files. Replace with brand-correct colors: focus rings → coral, primary action buttons → navy/umber, text highlights → coral.

Purpose: Brand palette enforcement — no blue-* anywhere in the codebase. These files predate Phase 7 palette enforcement.
Output: Four clean files with zero blue-* Tailwind classes.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-itinerary-page-with-map-integration-and-hotel-details/09-RESEARCH.md

<interfaces>
<!-- Brand palette (Tailwind custom colors configured in project): -->
<!-- navy = #285185 → text-navy, bg-navy, border-navy -->
<!-- coral = #D67940 → text-coral, bg-coral, border-coral, focus:ring-coral -->
<!-- umber = #6F4849 → text-umber, hover:text-umber -->
<!-- sky = #CCD9E2 → border-sky, bg-sky -->
<!-- sand = #F5EDE3 → bg-sand -->

<!-- Confirmed violations from RESEARCH.md: -->

<!-- src/app/(authenticated)/itinerary/[id]/page.tsx: -->
<!-- border-blue-500 → border-coral -->
<!-- hover:text-blue-700 → hover:text-coral -->
<!-- text-blue-600 (destination) → text-coral -->
<!-- focus:ring-blue-500 → focus:ring-coral -->

<!-- src/app/(authenticated)/itinerary/new/page.tsx: -->
<!-- focus:ring-blue-500 (×4 inputs) → focus:ring-coral -->
<!-- bg-blue-600 + hover:bg-blue-700 (submit button) → bg-navy + hover:bg-umber -->

<!-- src/components/itinerary/ActivityForm.tsx: -->
<!-- focus:ring-blue-500 (×4 inputs) → focus:ring-coral -->
<!-- bg-blue-600 + hover:bg-blue-700 (save button) → bg-navy + hover:bg-umber -->

<!-- src/components/itinerary/ActivityRow.tsx: -->
<!-- text-blue-600 (time display) → text-coral -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix blue violations in itinerary detail page and new itinerary page</name>
  <files>src/app/(authenticated)/itinerary/[id]/page.tsx, src/app/(authenticated)/itinerary/new/page.tsx</files>
  <action>
In `src/app/(authenticated)/itinerary/[id]/page.tsx`, apply these exact replacements:
- `border-b-2 border-blue-500` → `border-b-2 border-coral`
- `hover:text-blue-700` → `hover:text-coral`
- `text-blue-600` (on destination `<p>`) → `text-coral`
- `focus:ring-blue-500` (on description textarea) → `focus:ring-coral`

The bg-gray-50 on the outer div is not blue — leave it for now (full redesign happens in Plan 04).

In `src/app/(authenticated)/itinerary/new/page.tsx`, apply these exact replacements:
- All four input/select `focus:ring-blue-500` → `focus:ring-coral`
- Submit button: `bg-blue-600` → `bg-navy`, `hover:bg-blue-700` → `hover:bg-umber`

Do not change any logic, structure, or gray classes — only blue → brand color substitutions.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/itinerary-detail.test.tsx</automated>
  </verify>
  <done>grep -rn "blue-" src/app/(authenticated)/itinerary/ returns no matches; existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix blue violations in ActivityForm and ActivityRow</name>
  <files>src/components/itinerary/ActivityForm.tsx, src/components/itinerary/ActivityRow.tsx</files>
  <action>
In `src/components/itinerary/ActivityForm.tsx`, apply these exact replacements:
- All four input/textarea `focus:ring-blue-500` → `focus:ring-coral`
- Save button: `bg-blue-600` → `bg-navy`, `hover:bg-blue-700` → `hover:bg-umber`

In `src/components/itinerary/ActivityRow.tsx`, apply this exact replacement:
- Time display `text-blue-600` → `text-coral`

Do not change any logic, structure, or gray classes — only blue → brand color substitutions.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/activity-row.test.tsx</automated>
  </verify>
  <done>grep -rn "blue-" src/components/itinerary/ returns no matches; existing tests pass</done>
</task>

</tasks>

<verification>
Full blue-class audit:
- `grep -rn "blue-" src/app/(authenticated)/itinerary/ src/components/itinerary/` returns no output
- `npx vitest run` — full suite passes
</verification>

<success_criteria>
- Zero blue-* Tailwind classes in all four files
- All focus rings use focus:ring-coral
- All primary buttons use bg-navy with hover:bg-umber
- All time/destination text highlights use text-coral
- Existing tests pass with no regression
</success_criteria>

<output>
After completion, create `.planning/phases/09-itinerary-page-with-map-integration-and-hotel-details/09-02-SUMMARY.md`
</output>
