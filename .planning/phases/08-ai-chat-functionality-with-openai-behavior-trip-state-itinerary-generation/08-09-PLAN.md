---
phase: 08-ai-chat-functionality-with-openai-behavior-trip-state-itinerary-generation
plan: 09
type: execute
wave: 7
depends_on: []
files_modified:
  - src/lib/ai/prompts/trip-planner.ts
autonomous: true
requirements:
  - AI-PROMPT-01

must_haves:
  truths:
    - "AI never includes restaurants, cafes, or food-related venues in the activities array — only in daily_food"
    - "Every non-hotel activity description includes: what the traveler will be doing, a few things to see/do there, and how to get there from the previous stop"
    - "Activity descriptions are written in a pleasant expert travel-writer style, not a dry list"
    - "Each day has denser activity coverage — at least 4 non-hotel activities per day"
    - "Interesting facts appear in activity descriptions when genuinely present for the place"
    - "Duration estimate is included per activity (e.g. '2–3 hours')"
    - "No hard-coded date examples ('13th to 15th March') or city examples ('I'm guessing London') in the prompt"
    - "Transportation bridge between consecutive activities is included (e.g. 'Take a 10-minute taxi to...' or 'Walk 15 min along the waterfront to...')"
  artifacts:
    - path: "src/lib/ai/prompts/trip-planner.ts"
      provides: "Updated prompt rules with rich activity detail, transport bridging, food separation, and removed hard-coded examples"
  key_links:
    - from: "src/lib/ai/prompts/trip-planner.ts"
      to: "src/app/api/chat/message/route.ts"
      via: "buildTripPlannerPrompt() used in system prompt"
      pattern: "buildTripPlannerPrompt"
---

<objective>
Upgrade the AI trip planner prompt so it generates expert-quality itineraries:
- Rich activity descriptions in travel-writer style (things to do, facts, duration, transport bridges)
- Strict separation: activities section never contains food/restaurants (those go only in daily_food)
- Denser daily schedules (4+ activities per day)
- Context-aware intake questions — no hard-coded date/city examples

Purpose: Users currently get thin single-line activity descriptions and occasionally see restaurants mixed into the activity list. This closes the gap between the current output and a real travel-expert recommendation.
Output: Updated src/lib/ai/prompts/trip-planner.ts — only TRIP_PLANNER_RULES and the inline question examples are changed. All other exports remain untouched.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ai-chat-functionality-with-openai-behavior-trip-state-itinerary-generation/08-CONTEXT.md

<interfaces>
<!-- src/lib/ai/prompts/trip-planner.ts — current exports (DO NOT rename or remove) -->
export const TRIP_PLANNER_PERSONA: string
export const TRIP_PLANNER_RULES: string
export function buildTripPlannerPrompt(tripStateJson: string, phase: string): string

<!-- Used in src/lib/ai/system-prompt.ts -->
import { buildTripPlannerPrompt } from './prompts/trip-planner'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TRIP_PLANNER_RULES for expert activity quality and food separation</name>
  <files>src/lib/ai/prompts/trip-planner.ts</files>
  <action>
Update TRIP_PLANNER_RULES in `src/lib/ai/prompts/trip-planner.ts`. Preserve the overall structure but make the following targeted changes:

**1. Remove hard-coded examples from intake questions.**

Replace:
```
- **When are you planning to go?** (e.g. 13–16 March, or 'sometime in June') — skip if already known
- **Where are you flying from?** (I'm guessing London, but let me know if you've moved since we last spoke.) — skip if already known
```

With:
```
- **When are you planning to go?** (Specific dates, or a rough window — we can always adjust later.) — skip if already known
- **Where are you flying from?** — skip if already known
```

**2. Update the intro line template** to remove the hard-coded city guess pattern — it must never imply a default origin city. Replace:
```
"[Destination reaction]. To get our [N]-day adventure started, I just need a few more details:"
```
With:
```
"[Destination reaction]. To build the perfect itinerary, I just need a few more details:"
```

**3. Add a new section: Activity Description Rules.** Add it after the Time Format Rules section:

```
## Activity Description Rules — STRICT
- NEVER include restaurants, cafes, coffee shops, bars, food markets, or any eating/drinking venue as an activity_type: "activity" entry. All food recommendations belong ONLY in daily_food. This rule has no exceptions.
- Each activity description MUST include all of the following:
  1. What the traveler will be doing there (1–2 sentences, present tense, "you" address)
  2. A few highlights: 2–3 specific things to see, experience, or do at this location (bullet points inside the description or inline)
  3. A transport bridge from the PREVIOUS activity: how to get there and how long it takes (e.g. "Walk 15 min along the waterfront to reach it from the previous stop." or "Take a 10-minute taxi from the harbour.")
  4. Duration estimate inline in the description (e.g. "Allow 2–3 hours here.")
  5. An interesting fact when genuinely present for this place — skip if nothing noteworthy.
- Write in an expert travel-writer style: specific, confident, slightly evocative. NOT a dry list.
- Density: each day must have at least 4 non-hotel activities (aim for 4–6). A full day should feel full.
- The 'tips' field (separate from description): one practical logistic tip only — e.g. "Book skip-the-line tickets online." Leave null for most activities; do NOT force a tip on every one.
- Example description (Sagrada Família, Barcelona):
  "You'll spend the morning inside one of the world's most extraordinary buildings, still under construction after 140 years. Highlights: the jaw-dropping interior forest of branching columns, the Nativity façade's sculptural detail, and the rooftop towers with city views. Walk 20 min from your hotel or take a 5-minute taxi from Las Ramblas. Allow 1.5–2 hours inside. Interesting fact: Gaudí is buried in the crypt beneath the main nave."
```

**4. Expand the Daily Food Rules** to be extra explicit that activities must not include food:

Add at the start of the existing Daily Food Rules section:
```
- CRITICAL: The daily_food array is the ONLY place to put restaurant and food recommendations. The activities array must contain zero food/dining entries. Do not include "dinner at [restaurant]" or "lunch at [café]" as activities.
```

Keep all other sections (Hotel Accommodation Rules, Time Format Rules, Flights Rules, Formatting Rules, Phase Rules) exactly as they are.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "prompts/trip-planner" || echo "No TS errors in trip-planner"</automated>
  </verify>
  <done>
- TRIP_PLANNER_RULES no longer contains "13–16 March", "sometime in June", or "I'm guessing London"
- Activity Description Rules section exists with food-exclusion rule and rich description requirements
- Daily Food Rules section starts with the CRITICAL food-only reminder
- buildTripPlannerPrompt still exports correctly
- npx tsc --noEmit passes
  </done>
</task>

</tasks>

<verification>
`grep -c "I'm guessing London" src/lib/ai/prompts/trip-planner.ts` returns 0.
`grep -c "Activity Description Rules" src/lib/ai/prompts/trip-planner.ts` returns 1.
`grep -c "CRITICAL" src/lib/ai/prompts/trip-planner.ts` returns at least 1.
`npx tsc --noEmit` passes.
</verification>

<success_criteria>
- No hard-coded date/city examples in intake question bullets
- Activity Description Rules section present with food-exclusion and rich-description requirements
- Daily Food Rules starts with CRITICAL separation reminder
- All existing exports (TRIP_PLANNER_PERSONA, TRIP_PLANNER_RULES, buildTripPlannerPrompt) intact
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-chat-functionality-with-openai-behavior-trip-state-itinerary-generation/08-09-SUMMARY.md`
</output>
