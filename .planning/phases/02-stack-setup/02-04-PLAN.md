---
phase: 02-stack-setup
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - .env.local.example
autonomous: false
requirements:
  - STACK-06

must_haves:
  truths:
    - "App is live at a vercel.app URL"
    - "Visiting the Vercel URL without being logged in redirects to /login"
    - "Email/password registration works on the production Vercel deployment"
    - "Google OAuth works on the production Vercel deployment (callback URL registered)"
    - "No environment variables are missing — Supabase and auth flows function end-to-end in production"
  artifacts:
    - path: ".env.local.example"
      provides: "Updated env var template confirming all required keys"
  key_links:
    - from: "Vercel project settings"
      to: "NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY / OPENAI_API_KEY"
      via: "Vercel Environment Variables → Production"
    - from: "Google Cloud Console"
      to: "https://your-app.vercel.app/auth/callback"
      via: "Authorized redirect URIs"
    - from: "Supabase Authentication → URL Configuration"
      to: "https://your-app.vercel.app/auth/callback"
      via: "Redirect URLs allowlist"

user_setup:
  - service: vercel
    why: "Deployment to Vercel requires linking the GitHub repo and setting environment variables in the Vercel dashboard"
    dashboard_config:
      - task: "Import the repo to Vercel"
        location: "https://vercel.com/new → Import Git Repository"
        notes: "Select the barabula repo. Vercel auto-detects Next.js. Framework preset: Next.js. Root directory: leave as default (repo root, where next.config.ts is). Click Deploy — the first deploy will fail or be incomplete until env vars are set."
      - task: "Add environment variables"
        location: "Vercel Dashboard → Project → Settings → Environment Variables"
        notes: "Add all four: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY (or NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY), SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY. Set scope to Production (and Preview if desired). After adding, trigger a new deploy."
      - task: "Add production callback URL to Google Cloud Console"
        location: "https://console.cloud.google.com → APIs & Services → Credentials → your OAuth client"
        notes: "Add https://your-app.vercel.app/auth/callback to Authorized redirect URIs (replace your-app with the actual Vercel-assigned subdomain from the deployment URL)."
      - task: "Add production callback URL to Supabase"
        location: "Supabase Dashboard → Authentication → URL Configuration → Redirect URLs"
        notes: "Add https://your-app.vercel.app/auth/callback to the allowlist."
---

<objective>
Deploy the Next.js app to Vercel with all environment variables configured and verify the complete auth flow works in production.

Purpose: Production deployment validates the stack migration end-to-end. After this plan, the old CRA frontend is fully superseded by a live Next.js app.
Output: A live Vercel deployment where users can register, log in (email or Google), and access the authenticated layout.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stack-setup/02-CONTEXT.md
@.planning/phases/02-stack-setup/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build passes and update .env.local.example</name>
  <files>.env.local.example</files>
  <action>
    First verify the production build passes locally — this catches TypeScript errors and misconfigured imports before they fail on Vercel:

    ```bash
    npm run build
    ```

    If the build fails, fix all errors before proceeding. Common issues:
    - TypeScript strict mode catches `undefined` that dev mode ignores
    - Missing `'use client'` on components using browser-only hooks
    - Importing server-only modules from client components

    After a clean build, update `.env.local.example` to reflect the final required env vars (in case the key name is PUBLISHABLE_KEY rather than ANON_KEY, per the 02-02 SUMMARY):

    ```
    # Supabase (get from: Supabase Dashboard → Settings → API)
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

    # Use ANON_KEY for older Supabase projects, PUBLISHABLE_KEY for new 2025 projects
    # (check which key type your project issued in the Supabase Dashboard)
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
    # NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...  (use this instead if applicable)

    # Server-only secrets (never prefix with NEXT_PUBLIC_)
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
    OPENAI_API_KEY=sk-...
    ```

    Commit all outstanding changes so Vercel deploys the latest code:
    ```bash
    git add -A
    git status  # Review what's being committed — ensure no .env.local is staged
    ```
    (Actual commit happens at plan output step via gsd-tools.)
  </action>
  <verify>
    <automated>cd /Users/asmeenray/projects/barabula && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build exits with code 0 (no errors); .env.local.example is up to date</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A production-ready Next.js build. This checkpoint covers both the Vercel deployment steps (human-only: linking repo, setting env vars in dashboard) and verification that the live deployment works end-to-end.
  </what-built>
  <how-to-verify>
    **Step 1: Deploy to Vercel**
    Follow the user_setup steps in the plan frontmatter:
    1. Import the repo at https://vercel.com/new
    2. Add all four environment variables in Vercel → Settings → Environment Variables
    3. Trigger a redeployment (Vercel Dashboard → Deployments → Redeploy, or push a commit)
    4. Wait for deployment to complete — check the build log for errors

    **Step 2: Note the Vercel URL**
    The app will be live at `https://your-app.vercel.app`. Note this URL — you need it for the OAuth redirect URI steps.

    **Step 3: Add production OAuth redirect URIs**
    - Google Cloud Console: add `https://your-app.vercel.app/auth/callback` to Authorized redirect URIs
    - Supabase Dashboard → Authentication → URL Configuration → Redirect URLs: add same URL

    **Step 4: Verify production auth flows**
    Open an incognito window and go to `https://your-app.vercel.app`:

    Test A: Visiting `/dashboard` redirects to `/login`
    Test B: Register a new account — lands on `/dashboard` with nav bar
    Test C: Sign in with Google — completes OAuth and lands on `/dashboard`
    Test D: Refresh the page while logged in — session persists (no redirect to login)

    **Step 5: Check for missing env var errors**
    If any test fails with a Supabase connection error, check:
    - Vercel → Functions tab → any failed invocations → logs
    - Vercel → Settings → Environment Variables → confirm all four are present and set to Production scope
  </how-to-verify>
  <resume-signal>Type "deployed" with the Vercel URL (e.g., "deployed https://barabula-abc123.vercel.app") when all four production tests pass, or describe which step failed</resume-signal>
</task>

</tasks>

<verification>
After deployment:
1. `https://your-app.vercel.app/dashboard` → redirects to `/login` (middleware works in production)
2. Email registration creates user in Supabase, lands on `/dashboard`
3. Google OAuth completes and lands on `/dashboard`
4. Build log in Vercel shows zero TypeScript/ESLint errors
5. No "Missing Supabase environment variables" errors in Vercel function logs
</verification>

<success_criteria>
- App deployed on Vercel and live at a vercel.app URL (STACK-06)
- All four required environment variables configured in Vercel production settings
- Email/password and Google OAuth auth flows work in production
- Phase 2 complete: Next.js + Supabase + Vercel stack is fully operational
</success_criteria>

<output>
After completion, create `.planning/phases/02-stack-setup/02-04-SUMMARY.md` summarizing:
- Vercel deployment URL
- All auth flows verified in production (check each)
- Environment variables confirmed present (list names, not values)
- Phase 2 complete — stack is live and ready for Phase 3 Core Pages
</output>
