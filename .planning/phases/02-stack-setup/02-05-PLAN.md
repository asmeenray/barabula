---
phase: 02-stack-setup
plan: "05"
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "No unused imports, dead variables, or unreachable code remain in the codebase"
    - "No commented-out code blocks remain (unless they are explanatory examples)"
    - "No leftover scaffolding, placeholder files, or TODO stubs from migration remain"
    - "TypeScript compiles clean with zero errors"
    - "All tests (if any) still pass after cleanup"
  artifacts: []
---

<objective>
Clean up all unused or unnecessary code introduced or left over after the Phase 2 stack migration.

Purpose: Prevents technical debt from accumulating before Phase 3 begins. The migration from CRA to Next.js + Supabase likely left stale imports, dead utility functions, placeholder comments, and scaffolding files.
Output: A lean, clean codebase with only the code that is actually used by the running application.
</objective>

<execution_context>
@/Users/asmeenray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/asmeenray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stack-setup/02-CONTEXT.md
@.planning/phases/02-stack-setup/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused imports and dead variables</name>
  <action>
    Run TypeScript compiler and ESLint to surface unused imports and variables:

    ```bash
    npx tsc --noEmit 2>&1
    npx eslint . --ext .ts,.tsx --rule '{"no-unused-vars": "warn", "@typescript-eslint/no-unused-vars": "warn"}' 2>&1 | grep "no-unused-vars"
    ```

    Manually remove any flagged unused imports, variables, or type aliases across all source files. Focus on:
    - `src/` directory (all .ts and .tsx files)
    - Any utility/helper files created during migration that are no longer called

    After cleanup, confirm TypeScript still compiles clean:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd /Users/asmeenray/projects/barabula && npx tsc --noEmit 2>&1; echo "exit:$?"</automated>
  </verify>
  <done>npx tsc --noEmit exits with code 0</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead files and leftover scaffolding</name>
  <action>
    Identify and remove files that are no longer referenced anywhere in the codebase:

    1. Check for any leftover CRA artifacts (public/index.html if not used by Next.js, src/index.tsx if replaced, etc.)
    2. Check for placeholder or example files created during setup that were never wired up
    3. Check for duplicate config files (e.g., .eslintrc.js alongside eslint.config.mjs)
    4. Remove any `// TODO`, `// FIXME`, or `// TEMP` comment blocks that were migration scaffolding

    Do NOT remove:
    - .env.local.example (intentional template)
    - Any config files actively used by Next.js, Tailwind, TypeScript, or ESLint
    - README or documentation files
  </action>
  <verify>
    <automated>cd /Users/asmeenray/projects/barabula && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build still exits with code 0 after file removal</done>
</task>

<task type="auto">
  <name>Task 3: Remove commented-out code blocks</name>
  <action>
    Search for commented-out code blocks (not explanatory comments) across the src/ directory:

    ```bash
    grep -rn "// " src/ | grep -v "node_modules" | head -50
    ```

    Remove blocks of commented-out code that:
    - Are old implementations replaced during migration
    - Are debugging console.log statements left in
    - Are dead branches or feature flags that were never activated

    Preserve comments that:
    - Explain non-obvious logic
    - Document intentional decisions (e.g., "// supabase requires this header")
    - Are part of .env.local.example as examples
  </action>
  <verify>
    <automated>cd /Users/asmeenray/projects/barabula && npx tsc --noEmit 2>&1; echo "exit:$?"</automated>
  </verify>
  <done>No commented-out code blocks remain; TypeScript still compiles clean</done>
</task>

<task type="auto">
  <name>Task 4: Final build verification</name>
  <action>
    Run a final clean build to confirm the cleanup didn't break anything:

    ```bash
    npm run build
    ```

    If the build fails, revert only the specific removal that caused the failure and re-run.
  </action>
  <verify>
    <automated>cd /Users/asmeenray/projects/barabula && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build exits with code 0 with a clean output</done>
</task>

</tasks>

<verification>
After cleanup:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — exits with code 0
3. No unused imports remain (ESLint no-unused-vars shows no warnings)
4. No leftover migration artifacts or placeholder files in the repo
</verification>

<success_criteria>
- Codebase contains only code that is actively used
- TypeScript compiles clean with zero errors
- Production build passes without errors
- No dead files, stale imports, or commented-out code blocks from the migration remain
</success_criteria>

<output>
After completion, create `.planning/phases/02-stack-setup/02-05-SUMMARY.md` summarizing:
- Files removed and why
- Imports/variables cleaned up (count or list)
- Final build status confirmed clean
- Phase 2 fully complete and codebase is lean for Phase 3
</output>
